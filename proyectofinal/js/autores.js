let listaAutores = [{
    'nombre': 'Gabriel ',
    'apellido': 'García Marquez',
}]; // se sacan con alt 91, cada valor tiene una posición e iniciaen 0;


for (let i = 0; i < listaAutores.length; i++) {
    console.log(listaAutores[i]);
}